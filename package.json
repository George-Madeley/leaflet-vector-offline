{
  "name": "leaflet-vector-offline",
  "version": "1.0.0",
  "description": "Plugin for Leaflet.js that supports offline vector map tiles.",
  "main": "src/index.ts",
  "scripts": {
    "lint": "npx eslint src test",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "npx prettier src test --check",
    "prettier:fix": "npm run prettier -- --write",
    "format": "npm run prettier:fix && npm run lint:fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "George Madeley",
  "license": "ISC",
  "devDependencies": {
    "globals": "^15.8.0",
    "prettier": "3.3.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.offline": "^3.0.1",
    "pmtiles": "^3.0.6",
    "protomaps-leaflet": "^4.0.0"
  },
  "repository": "https://github.com/George-Madeley/leaflet-vector-offline.git"
}
